spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.7:3306/mall_ums?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: abc123
  application:
    name: member-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.7
        enabled: true
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8081

